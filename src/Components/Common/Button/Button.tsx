import React from 'react';
import styles from './Button.module.scss';

const Button = () => {
    return (
        <div>
            <button>Haha</button>
        </div>
    )
}

export default Button
